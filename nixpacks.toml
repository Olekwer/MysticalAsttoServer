[phases.setup]
nixPkgs = ["nodejs_18", "python3", "gcc", "gnumake"]

[phases.build]
cmds = [
    "npm ci --only=production",
    "npm run build",
    "npx prisma generate"
]

[phases.start]
cmd = "npm run start:prod"
