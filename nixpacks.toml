[phases.setup]
nixPkgs = ["nodejs_18", "python3", "gcc", "gnumake", "openssl", "pkg-config"]

[phases.build]
cmds = [
    "npm ci",
    "npx prisma generate",
    "npm run build",
    "npx prisma db push --accept-data-loss"
]

[phases.start]
cmd = "./start.sh"
