[phases.setup]
nixPkgs = ["nodejs_18", "python3", "gcc", "gnumake", "openssl", "pkg-config"]

[phases.build]
cmds = [
    "npm ci",
    "npx prisma generate",
    "npm run build"
]

[phases.start]
cmd = "npm run start:prod"
